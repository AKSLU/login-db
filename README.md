# 🟩 Login-DB (Flask + PostgreSQL)

Простой проект авторизации на **Flask** с подключением к базе данных **PostgreSQL**.
Пользователь вводит **логин** и **пароль**, Flask проверяет данные в таблице `users`.
При успешном входе отображается страница приветствия, иначе — ошибка.

---

## 📁 Структура проекта

```
login-db/
│
├── app.py
├── requirements.txt
│
└── templates/
    ├── login.html
    ├── error.html
    └── welcome.html
```

---

## 🔧 Установка и запуск проекта

### 1. Установи зависимости

Создай виртуальное окружение (не обязательно, но желательно):

```bash
python -m venv venv
source venv/bin/activate    # Linux / macOS
venv\Scripts\activate       # Windows
```

Установи необходимые пакеты:

```bash
pip install flask psycopg2
```

---

## 🗄 Настройка базы данных PostgreSQL

1. Создай базу данных:

```sql
CREATE DATABASE base;
```

2. Создай таблицу пользователей:

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(255) NOT NULL,
    password VARCHAR(255) NOT NULL
);
```

3. Добавь тестового пользователя:

```sql
INSERT INTO users (username, password)
VALUES ('admin', '1234');
```

---

## ⚙ Настройка подключения в `app.py`

Задай свои данные для подключения:

```python
conn = psycopg2.connect(
    dbname="base",
    user="postgres",
    password="ВАШ_ПАРОЛЬ",
    host="localhost"
)
```

---

## ▶ Запуск приложения

```bash
python app.py
```

Приложение по умолчанию доступно по адресу:

```
http://127.0.0.1:5000/login
```

---

## 📝 Описание маршрутов

### `/login`

Методы: `GET`, `POST`

✔ Показывает форму входа
✔ Проверяет логин и пароль в базе
✔ Перенаправляет на welcome.html
✘ Показывает ошибку при неверных данных

---

## 🖼 Шаблоны

* **login.html** — форма авторизации
* **error.html** — сообщение о неверных данных
* **welcome.html** — приветствие пользователя

---

## 📌 Примечание

Этот проект демонстрационный.
Для реального применения нужно:

* хешировать пароли (**bcrypt**, **werkzeug.security**)
* использовать сессии или JWT
* не хранить пароль в коде

---

